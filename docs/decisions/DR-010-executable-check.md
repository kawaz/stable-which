# DR-010: PATH 候補に実行ビットチェックを追加

## 決定

PATH 上の候補フィルタリングに Unix 実行ビット (`mode & 0o111`) チェックを追加する。入力バイナリ自体は `is_file()` のみ。

## 理由

- `is_file()` のみだと実行不能ファイルが候補になり、shell の `which` と結果が乖離する
- 入力バイナリはユーザーが明示指定したものなので、実行ビットがなくても分析対象にする
