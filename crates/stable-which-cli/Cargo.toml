[package]
name = "stable-which-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[[bin]]
name = "stable-which"
path = "src/main.rs"

[dependencies]
stable-which = { path = "../stable-which" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
